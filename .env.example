# ============================================
# WinningBet — Variabili d'Ambiente
# ============================================
# Copia questo file in .env e inserisci le tue API key.
#
#   cp .env.example .env
#
# Entrambe le chiavi sono obbligatorie per il funzionamento
# completo. Senza API_FOOTBALL_KEY nessun endpoint funzionera'.
# Senza FOOTBALL_DATA_KEY il meccanismo di fallback sara' disabilitato.
# ============================================

# API-Football (api-sports.io) — Provider primario
# Registrati su https://www.api-football.com/ per ottenere la chiave.
# Piano gratuito: 100 richieste/giorno. Piano a pagamento consigliato.
# Usata come header: x-apisports-key
API_FOOTBALL_KEY=

# football-data.org — Provider di fallback
# Registrati su https://www.football-data.org/ per ottenere il token.
# Piano gratuito: 10 richieste/minuto, dati aggiornati ogni ~1 minuto.
# Usata come header: X-Auth-Token
FOOTBALL_DATA_KEY=

# ============================================
# Supabase — Database + Auth
# ============================================
# Crea un progetto su https://supabase.com/dashboard
# Le chiavi si trovano in: Settings > API

# URL del progetto Supabase
SUPABASE_URL=

# Anon key (pubblica, usata lato client per auth)
SUPABASE_ANON_KEY=

# Secret key (segreta, usata solo lato server — bypassa RLS)
SUPABASE_SECRET_KEY=

# ============================================
# Anthropic — AI Prediction Engine
# ============================================
# Registrati su https://console.anthropic.com/
# Genera una API key in Settings > API Keys
ANTHROPIC_API_KEY=

# ============================================
# Cron — Sicurezza endpoint di generazione
# ============================================
# Segreto condiviso per autenticare chiamate cron
# Genera un valore casuale: openssl rand -hex 32
CRON_SECRET=

# ============================================
# Stripe — Pagamenti
# ============================================
# Crea un account su https://dashboard.stripe.com/
# Usa le chiavi di test durante lo sviluppo

# Secret key (lato server)
STRIPE_SECRET_KEY=

# Webhook signing secret (da Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET=

# Price ID per il piano PRO (€9.99/mese)
STRIPE_PRO_PRICE_ID=

# Price ID per il piano VIP (€29.99/mese)
STRIPE_VIP_PRICE_ID=

# ============================================
# Telegram — Tip Delivery
# ============================================
# Crea un bot via @BotFather su Telegram

# Token del bot Telegram
TELEGRAM_BOT_TOKEN=

# Chat ID del canale pubblico (tips free)
TELEGRAM_PUBLIC_CHANNEL_ID=

# Chat ID del canale privato (tips pro/vip)
TELEGRAM_PRIVATE_CHANNEL_ID=

# Username del bot (senza @) — usato per generare deep link
TELEGRAM_BOT_USERNAME=

# Secret per verificare i webhook Telegram (impostato via setWebhook)
# Genera un valore casuale: openssl rand -hex 32
TELEGRAM_WEBHOOK_SECRET=

# ============================================
# SMTP — Email (Nodemailer)
# ============================================
# Configura con il server SMTP del tuo dominio

# Hostname del server SMTP (es. smtp.tuodominio.it)
SMTP_HOST=

# Porta SMTP (465 per SSL, 587 per STARTTLS)
SMTP_PORT=465

# Credenziali SMTP
SMTP_USER=
SMTP_PASS=

# Indirizzo mittente (es. info@winningbet.it)
SMTP_FROM=
